{% extends 'BlackRookTaskBundle::layout.html.twig' %}
{% block body %}

<h1>Project creation</h1>

<form class="entity" action="{{ path('project_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_errors(form) }}
    {{ form_rest(form) }}
    <div class="actions">
        <button type="submit">Create</button>
    </div>
</form>

{% endblock body %}